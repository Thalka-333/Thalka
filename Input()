# Read x and k from the first line
x, k = map(int, input().split())

# Read the polynomial string from the second line
polynomial = input()

# Use eval() to calculate the result of the polynomial string
# It will use the 'x' variable we defined above
result = eval(polynomial)

# Print True if the result matches k, else False
print(result == k)
