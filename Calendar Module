import calendar

# Read month, day, and year from input
m, d, y = map(int, input().split())

# Get the day index (0-6)
day_index = calendar.weekday(y, m, d)

# Convert index to uppercase day name
print(calendar.day_name[day_index].upper())
